---
title: 서버 컴포넌트에서 데이터를 가져오는 자리
date: 2026-08-22
summary: 아직 정리가 안 끝난 글. draft 플래그 동작 확인용으로도 쓰인다.
tags: ['next.js', 'rsc']
draft: true
---

서버 컴포넌트에서 데이터를 어디서 가져와야 하는지에 대한 메모. 아직 결론이 없다.

## 컴포넌트 안에서 직접 부르기

props 로 내려보내는 대신 필요한 컴포넌트가 직접 가져오는 쪽이 응집도가 높다.
다만 같은 데이터를 두 컴포넌트가 부르면 중복 요청이 된다.

## 아직 안 풀린 것

- 요청 단위 캐시를 어디까지 믿을 수 있는지
- 에러 경계를 컴포넌트마다 두는 게 맞는지

여기까지 쓰고 멈췄다.
